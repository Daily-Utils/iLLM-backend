# Use an official Ubuntu runtime as a parent image
FROM ubuntu:20.04

# Install dependencies
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Expose the port Ollama API will run on
EXPOSE 11434

# Copy the entry point script
COPY ollamaEntryPoint.sh /app/ollamaEntryPoint.sh

# Make the entry point script executable
RUN chmod +x /app/ollamaEntryPoint.sh

# Set the entry point
ENTRYPOINT ["/app/ollamaEntryPoint.sh"]